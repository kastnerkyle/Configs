Make a directory called ~/.xmonad
Place the file named .xmonad.hs in ~/.xmonad
Copy the file xmobarrc to ~/.xmobarrc
Depending on the computer being used, the entries for Wireless or Network may be removed
